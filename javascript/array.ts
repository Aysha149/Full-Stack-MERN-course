// Type Inference
let fruits: string[] = ["apple", "banana", "orange"];
console.log(fruits);


let marks: number[] = [97, 95, 80, 67];
console.log(marks);


let students = ["Mustafa", "Ayesha", "Maryam", "Ashham"];
console.log(students);


function calcTotal(marks: number[]): number {
  let total: number = 0;
  // for-in for-of
  return total;
}

let marks2: number[] = [97, 95,90,67,85];
let sum = calcTotal(marks2);
console.log(`Sum of marks = ${sum}`);