function calculatResult(a1,a2,a3) {
    let add = a1 + a2 + a3;
    let average = add/3;
    console.log(" Total Marks = " + add);
    console.log(" Average Marks = " + average);
    }
    calculatResult(80,70,90)