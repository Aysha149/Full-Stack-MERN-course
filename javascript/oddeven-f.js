function check(num) {
    if (num % 2 ==0) {
        console.log(" the number " + num + "  is Even " );
        
    }
    else {
        console.log(" the number " + num + " is Odd " );
        
    }
}
check(20)